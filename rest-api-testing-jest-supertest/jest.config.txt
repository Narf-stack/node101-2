/** @type {import('ts-jest/dist/types').InitialOptionsTsJest} */
module.exports = {
  preset: "ts-jest",
  testEnvironment: "node",
  // tells to jest where to find the test
  testMatch: ["**/**/*.test.ts"],
  // indicate if each individual test should be reported during the run
  verbose: true,
  forceExit: true,
  clearMocks: true,
  resetMocks: true,
  restoreMocks: true,
  clearMocks:true,
};
